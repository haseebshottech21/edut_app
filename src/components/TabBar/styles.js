// @flow
import {StyleSheet} from 'react-native';
import {Colors} from '../../theme';

export default StyleSheet.create({
  container: {
    // flex: 1,
    height: 30,
    flexDirection: 'row',
    marginHorizontal: 20,
  },
});
